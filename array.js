// array is a data structure that can hold a collection of values
// can hold mix of data

const arr = [1, 2, 3, 4, 5];
arr.push(6);
arr.unshift(0); // add from the start
console.log(arr);
arr.pop();
console.log(arr);
arr.shift(); // remove from the end
console.log(arr);
